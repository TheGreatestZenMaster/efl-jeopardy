<%= form_for(@game, :url => sessions_path) do |f| %>
    <%= f.hidden_field :id, :value => 1 %>
    <%= f.fields_for :team do |t| %>
        <%= t.text_field :team1 %>
        <%= t.text_field :team2 %>
    <% end %>
    <%= f.submit "Start"%>
<% end %>